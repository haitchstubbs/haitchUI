// Placeholder resolver for code templates used by the renderer.
// The real project may populate this map at build or runtime; an empty map satisfies TypeScript for now.
import type React from "react";

export const codeTemplates: Record<string, React.ComponentType<any>> = {};
